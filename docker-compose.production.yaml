version: "3.8"

services:
  api:
    image: nayeembd/simple-node-app:${COMMIT_HASH}
    deploy:
      replicas: 2
      update_config:
        parallelism: 1
        order: start-first
        failure_action: rollback
    restart: on-failure:10
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 2s
    networks:
      - mynodeapp

  nginx:
    image: nginx:alpine
    ports:
      - 80:80
    networks:
      - mynodeapp
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - api


networks:
  mynodeapp:
